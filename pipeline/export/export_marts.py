# Runs the staging + mart SQL against DuckDB, sanity-checks the results
# (row counts, known anchors like national sales ~1.5-2M/yr), and writes
# the final CSV/JSON files into ../../public/data/ for the site to read.
#
# TODO(session 3+).
